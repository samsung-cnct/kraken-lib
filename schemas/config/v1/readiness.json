{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "http://judkins.house/apis/k2/v1/readiness.json",
  "$$target": "readiness.json",
  "title": "Cluster Readiness Configuration",
  "description": "Options to determine when a cluster is ready.",

  "properties": {
    "type": {
      "description": "The type of test that should be used to determine if a cluster is ready.",
      "type": "string"
    },
    "value": {
      "description": "The value for the test.",
      "type": "number"
    },
    "wait": {
      "description": "The number of seconds to wait for the test to complete.",
      "type": "number"
    }
  },
  "required": [
    "type",
    "value",
    "wait"
  ],
  "additionalProperties": false,
  "type": "object"
}

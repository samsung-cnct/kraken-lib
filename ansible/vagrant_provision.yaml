---
- hosts: cluster
  gather_facts: False
  roles:
    - { role: sigma.coreos-bootstrap, coreos_pypy_version: '2.6.1' }

- hosts: cluster
  gather_facts: True
  roles:
    - kubernetes

- hosts: localhost
  sudo: false
  roles:
    - ssh-config

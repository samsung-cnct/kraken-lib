[Unit]
Description=Install kubectl client tool
Requires=network-online.target
After=network-online.target

[Service]
ExecStart=/usr/bin/wget -N -O /opt/bin/kubectl.pending {{kubernetes_binaries_uri}}/kubectl
Type=oneshot
RemainAfterExit=true

{
  "apiVersion": "v1",
  "kind": "Pod",
  "metadata": {
    "name": "k8s-master"
  },
  "spec": {
    "hostNetwork": true,
    "containers": [
      {
        "name": "scheduler",
        "image": "{{hyperkube_image}}",
        "command": [
          "/hyperkube",
          "scheduler",
          {% for k,v in kube_scheduler.iteritems() %}
            "--{{k}}={{v}}",
          {% endfor %}
          ""
        ]
      }
    ]
  }
}

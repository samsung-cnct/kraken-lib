- path: /etc/kubernetes/kubeconfig.yaml
  encoding: "gz+b64"
  content: |
    {{ lookup('file', '{{ config_base }}/{{ cluster.name }}/{{ node.name }}.kubeconfig.manifest' | expanduser) | gz | b64encode }}
